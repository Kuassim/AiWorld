name: Test Secrets

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Test user secret
      run: |
        echo "Tenancy length: $(echo '${{ secrets.OCI_CLI_TENANCY}}' | wc -c)"
        echo "Tenancy value: ${{ secrets.OCI_CLI_TENANCY}}"
