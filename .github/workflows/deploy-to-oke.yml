name: Test Secrets

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Test region secret
      run: |
        echo "Region length: $(echo '${{ secrets.OCI_REGION }}' | wc -c)"
        echo "Region value: ${{ secrets.OCI_REGION }}"
