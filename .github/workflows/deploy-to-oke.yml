name: Test Secrets

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Test user secret
      run: |
        echo "Region length: $(echo '${{ secrets.OCI_CLI_USER}}' | wc -c)"
        echo "Region value: ${{ secrets.OCI_CLI_USER}}"
